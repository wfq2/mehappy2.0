import { DocumentLoader, DocumentFile } from './document-loader';
import { DocumentNode } from 'graphql';
import { ExtractOptions } from '../../utils/extract-document-string-from-code-file';
export declare class DocumentsFromGlob implements DocumentLoader {
    canHandle(doc: string): Promise<boolean> | boolean;
    documentsFromGlobs(documentGlob: string): Promise<string[]>;
    loadFileContent(filePath: string, options?: ExtractOptions): DocumentNode | null;
    loadDocumentsSources(filePaths: string[], options?: ExtractOptions): DocumentFile[];
    handle(doc: string, options?: ExtractOptions): Promise<DocumentFile[]>;
}
