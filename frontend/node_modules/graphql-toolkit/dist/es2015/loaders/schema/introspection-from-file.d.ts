import { GraphQLSchema } from 'graphql';
import { SchemaLoader } from './schema-loader';
export declare class IntrospectionFromFileLoader implements SchemaLoader {
    stripBOM(content: string): string;
    parseBOM(content: string): any;
    canHandle(pointerToSchema: string): boolean;
    handle(pointerToSchema: string, _options?: any): Promise<GraphQLSchema>;
}
