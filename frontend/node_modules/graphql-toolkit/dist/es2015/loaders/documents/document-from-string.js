import * as isValidPath from 'is-valid-path';
import { parse } from 'graphql';
export class DocumentFromString {
    canHandle(doc) {
        if (isValidPath(doc)) {
            return false;
        }
        try {
            parse(doc);
            return true;
        }
        catch (e) {
            return false;
        }
    }
    handle(doc, _options) {
        return [{ filePath: 'document.graphql', content: parse(doc) }];
    }
}
//# sourceMappingURL=document-from-string.js.map